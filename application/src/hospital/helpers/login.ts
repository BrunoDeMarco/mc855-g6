import axios from "axios";

export const login = (username: string, password: string) => {
  // TODO fix to call login api
  // return axios.post("http://localhost:5050/session", { username, password });
};
